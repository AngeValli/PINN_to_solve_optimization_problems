[project]
name = "pinn-to-solve-optimization-problems"
version = "0.1.0"
description = "This is a step-by-step guide explaining how to implement a physics-informed neural network to solve optimization problems."
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "flax==0.12.0",
    "jax==0.8.2",
    "jaxlib==0.8.2",
    "matplotlib==3.9.2",
    "numpy==2.2.6",
    "optax==0.2.5",
]
